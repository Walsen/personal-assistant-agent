[project]
name = "infra"
version = "0.1.0"
description = "CDK app that provisions AWS prerequisites (S3 sessions bucket, Secrets Manager secret) for deploying the personal assistant agent to Amazon Bedrock AgentCore Runtime."
authors = [
    { name = "Sergio Rodriguez", email = "sergio@ffactory.io" }
]
requires-python = ">=3.12"
dependencies = [
    "aws-cdk-lib>=2.170.0",
    "constructs>=10.4.0",
]

[tool.uv]
package = false
