[project]
name = "infra"
version = "0.1.0"
description = "CDK app that provisions the web chat interface (Lambda proxy + S3/CloudFront static frontend) for the personal assistant agent deployed in 06-agente-AgentCore-deploy."
authors = [
    { name = "Sergio Rodriguez", email = "sergio@ffactory.io" }
]
requires-python = ">=3.12"
dependencies = [
    "aws-cdk-lib>=2.170.0",
    "constructs>=10.4.0",
]

[tool.uv]
package = false
